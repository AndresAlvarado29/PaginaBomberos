import { llaves } from "src/apikeys/llaves";
export const environment = {
    keyMailService:llaves.keyMailService,
    keyTemplateService: llaves.keyTemplateService,
    PublicKey: llaves.PublicKey,
    key:llaves.key,
};
